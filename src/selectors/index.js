export const working = state => state.working;

export const addressResults = state => state.results.addresses;

export const priceEstimate = state => state.results.priceEstimate;

export const failed = state => state.failed;
